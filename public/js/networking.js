var networking = {
  init: function (argument) {
    // body...
  },

  // -- by id -- //
  switch(action){
    case 'setGroup':
      var name_group = $(it).text();
      $('#titleGroup').empty().append(name_group);
      $("input[name='nameGroup']").val(name_group);
      break;
    case 'addList':


}